{
  "name": "mountebank-poc",
  "version": "1.0.0",
  "description": "Creating a mock http instance to run UI tests against",
  "main": "index.js",
  "scripts": {
    "start": "mb --configfile templates/imposters.ejs --allowInjection",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Reginald Ross",
  "license": "ISC",
  "dependencies": {
    "mountebank": "^2.2.1",
    "prettier": "^2.0.5"
  }
}
